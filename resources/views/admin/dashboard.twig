{% extends 'admin/admin.twig' %}
{% block title %} Dashboard {% endblock %}
{% block description %}{% endblock %}

{% block content %}

    <div>
        <div class="container mt-3">
            <div class="row">
                <div class="col-md-4">
                    <h6>Welcome {{ auth_user().email }}</h6>
                    <p>Content Management Sections</p>
                </div>
                <div class="col-md-8">

                    <div class="list-group">
                        <a href="{{ route('camps.index') }}" class="list-group-item
                        list-group-item-action">
                            Camps
                            <span class="label label-success float-right">{{ count }}
                                camps in the database</span></a>
                        <a href="{{ route('prices.index') }}" class="list-group-item
                        list-group-item-action">Pricing</a>
                        <a href="#" class="list-group-item list-group-item-action">Testimonials</a>

                    </div>
                </div>
            </div>

        </div>
    </div>


{% endblock %}